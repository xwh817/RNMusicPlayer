import React, {Component} from 'react';
import {Text, View} from 'react-native';

export default class PlayList extends Component {
    render(){
        return (<View><Text>PlayList</Text></View>);
    }
}